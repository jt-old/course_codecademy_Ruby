# declare array
s = [["ham", "swiss"], ["turkey", "cheddar"], ["roast beef", "gruyere"]]

# iterate over each element of s array through a do loop
# store elements in sub_array
s.each do |sub_array|
  # iterate through each element in sub array through a do loop
  sub_array.each do |x|
  # puts out each result
  puts x
  # end sub_array do loop
  end
  # end s.each do loop
end
